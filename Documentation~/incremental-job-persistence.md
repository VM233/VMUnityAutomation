# Incremental job persistence

Profiler frame `3301` in the MarbleBattlers Editor capture of 2026-09-12 attributes 525.053 ms and 50.4 MB of allocations to `VmAutomationPersistentJobRunner.Tick`. Frames `3194` and `3198` repeat the same 50.4 MB allocation. Every step serializes the full retained execution list and the full published history before and after the project callback, even when only one small progress record changes. The frozen store has 200 execution records (1,859,996 bytes, largest 127,166 bytes) and 200 history records (1,241,289 bytes, largest 126,938 bytes). This is distinct from the sandbox preparation and first-use asset loading stalls.

Entry points are start, progress, cancellation, completion, cleanup and reload recovery. The persistent runner remains the sole execution-state writer. JobHistory remains the sole owner of immutable public snapshots and access capabilities. A shared record store owns only atomic JSON persistence. Each job type and ID names one hashed record. Updating a job serializes and replaces that record only. An ordered index owns membership, with atomic replacement only when membership/order changes. Removed record files retire after index publication. Existing public commands, tokens, job identity, cancellation and interrupted-job semantics remain unchanged.

The existing aggregate file is migrated once when no record index exists. All records are written before the index is published, then the aggregate file retires. An interrupted migration has no published index and therefore reuses the original aggregate input. Once the index exists it is the sole authority, including after a process exit between index publication and aggregate retirement. Missing indexed records or invalid JSON fail at the store boundary. There is no history purge, deferred durability, background write, delayed progress or alternate transport.

Static Cost Ledger before executable writes: the frozen calibration has 200 records per owner and one changed record per progress publication. At most four changed-record serializations and atomic file replacements per ordinary runner step replace four full-store serializations. Each identity is at most 96 UTF-8 bytes in this frozen input, yielding at most 804 bounded SHA-256 operations and 77,184 input bytes per step. Largest changed-record admission is 128 KiB here, giving at most 512 KiB serialized payload per step (the current progress records are below 8 KiB). Existing history retention supports up to 2,000 records and execution retention is 200; index traversal remains linear, at most 4,404 identity hashes across both publications and below 512 KiB index/identity scratch. Membership changes serialize at most a 134 KiB index for that declared maximum. No Cartesian product, per-record Unity calls, physics work or native asset operations. Retained store state contains index strings only, below 512 KiB at maximum retention, plus the already-owned job dictionaries. All file replacements keep the existing flushed temporary-file publication and path lock. Migration is a single bounded load of the original files and one write per retained record, outside calibration sample execution. A load also visits at most the retained records plus the one interrupted membership publication to retire unindexed record files. PASS for removal of the history-size multiplier from recurring serialization. Actual disk latency and the remaining runtime owners still require a fresh capture.

Focused tests cover existing aggregate migration and readback, changing one record while another has exclusive file access (proving it is not rewritten), order/membership updates, pruning and interrupted-migration retirement. Integration must repeat the same official calibration request and cancellation/cleanup path, then compare the actual runner timing and allocations. Compile success alone does not close responsiveness.

The repeat capture `f5753bdb2618443184abd2fbbe84cca9` completed all twelve original cases. Its inspected frames show 0.8 MB and 18.787–32.119 ms per persistent-runner callback. Cancellation job `eca5f9e4af6841e490a38f805b22c7ac` retired its in-progress sample, completed explicit cleanup, preserved the terminal report bytes and left no Console error. The remaining persistence work still hashes every retained identity, and writes the same running state before each incremental step even though that state has already been durably published.

Identity-cache owner and lifecycle: this record store caches the pure mapping from the validated `(jobType, jobId)` string pair to its SHA-256 filename. A load populates it while verifying record identity. Ordinary publications reuse that mapping, membership retirement removes unused identities, and the store lifetime is the existing domain-owned persistence owner. Every record's identity fields and uniqueness are still checked at publication. No mutable job state or public snapshot is cached. The runner persists queued-to-running and cleanup-start transitions before invoking the handler. Already-running incremental steps retain their previously published state until their new progress or terminal outcome is atomically written. A reload between those boundaries is still interrupted, with no step replay.

Revised Static Cost Ledger before executable writes: at most 2,200 retained identities across both stores and 96 UTF-8 bytes per frozen identity. Normal progress performs two per-record serializations and atomic replacements, with at most 2,200 identity lookups, zero SHA-256 operations and zero index replacements. A new job requires one hash per store. Membership publication remains linear and retires unused mapping entries only after index publication. Added retained mapping storage is below 512 KiB at the declared maximum. Loading and migration keep their original bounded hash and I/O work. No background writer, delayed durability, new timer, Unity work or asymptotic loop increase. PASS for removing repeated identity allocation and duplicate unchanged-state publication. Actual disk latency remains observable and is not claimed to have a deterministic frame bound.
